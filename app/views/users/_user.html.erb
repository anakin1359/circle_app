<li>
  <%= image_tag("#{user.user_icon}", class: "users-icon", size: 50) %>
  <%= link_to user.name, user %>
  <% if current_user.admin? %>
    | <%= link_to "delete", user, method: :delete, data: { confirm: "選択したユーザを削除します。よろしいですか?" } %>
  <% end %>
</li>
